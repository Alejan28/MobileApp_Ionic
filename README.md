# Item Management & Location App (Ionic Version)

A cross-platform mobile application built using **Ionic Framework** and **Capacitor**. This project manages item data, captures images, and handles location-based features using web-based technologies.

## 🚀 Features

*   **Cross-Platform**: Single codebase for Android, iOS, and Web.
*   **Camera Integration**: Uses the `@capacitor/camera` plugin to capture and store item photos.
*   **Geolocation & Maps**: Integrated Leaflet or Google Maps JavaScript SDK to handle coordinate picking (Latitude/Longitude).
*   **Storage**: Uses **Ionic Storage** (or SQLite) for persistent data handling across sessions.
*   **Responsive UI**: Built with Ionic Components for a native look and feel on any device.

## 🛠 Tech Stack

- **Framework**: Ionic (Angular/React/Vue)
- **Runtime**: Capacitor
- **UI Components**: Ionic UI
- **Plugins**: 
    - `@capacitor/camera`: Photo capture and gallery access.
    - `@capacitor/geolocation`: Fetching device coordinates.
    - `@capacitor/preferences` (or SQLite): Local data persistence.
